name: backend tests

on: 
  push: 
      branches:
      - main
  pull_request:
    branches:
    - main
jobs:
  Backend-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: back

    steps:
      - name: Checkout
        uses: actions/checkout@v3
